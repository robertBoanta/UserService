apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8092
      protocol: TCP
      name: http
  selector:
    app: user-service
---
apiVersion: v1
kind: Service
metadata:
  name: user-service-nodeport
  labels:
    app: user-service
spec:
  type: NodePort
  ports:
    - port: 8092
      targetPort: 8092
      nodePort: 30090
      protocol: TCP
      name: http
  selector:
    app: user-service